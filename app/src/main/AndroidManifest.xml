<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.activity.myapplication">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/soccer"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />
        <activity
            android:name=".HomeActivity"
            android:label="@string/title_activity_home"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".GioiThieuDoiBongActivity" />
        <activity android:name=".QuanLiThanhVienActivity" />
        <activity android:name=".SoDoChienThuatActivity" />
        <activity android:name=".HoatDongTrongThangActivity" />
        <activity android:name=".sodochienthuat.SoDo4321Activity" />
        <activity android:name=".sodochienthuat.SoDo532Activity" />
        <activity android:name=".sodochienthuat.SoDo433Activity" />
        <activity android:name=".sodochienthuat.SoDo4231Activity" />
        <activity android:name=".sodochienthuat.SoDo235Activity" />
        <activity android:name=".sodochienthuat.SoDo4222Activity" />
        <activity android:name=".sodochienthuat.SoDo422Activity" />
        <activity android:name=".sodochienthuat.SoDo451Activity" />
        <activity android:name=".sodochienthuat.SoDo352Activity" />
        <activity android:name=".thang.Thang1Activity" />
        <activity android:name=".thang.Thang2Activity" />
        <activity android:name=".thang.Thang3Activity" />
        <activity android:name=".thang.Thang5Activity" />
        <activity android:name=".thang.Thang6Activity" />
        <activity android:name=".thang.Thang8Activity" />
        <activity android:name=".thang.Thang9Activity" />
        <activity android:name=".thang.Thang10Activity" />
        <activity android:name=".thang.Thang11Activity" />
        <activity android:name=".thang.Thang12Activity" />
        <activity android:name=".thang.Thang4Activity" />
        <activity android:name=".thang.Thang7Activity" />
        <activity android:name=".ThemListActivity" />
        <activity android:name=".TinTucTrongNgayActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps"></activity>
    </application>

</manifest>